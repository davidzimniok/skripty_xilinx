# Jednoduchý startup script
cat > /etc/rc.local << 'EOF'
#!/bin/bash
# Auto-start filebrowser
nohup /usr/local/bin/filebrowser --database /etc/filebrowser/filebrowser.db --address 0.0.0.0 --port 8080 --root /home > /var/log/filebrowser.log 2>&1 &
exit 0
EOF

chmod +x /etc/rc.local

# Test
/etc/rc.local
